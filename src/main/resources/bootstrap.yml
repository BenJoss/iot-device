spring:
  cloud:
    nacos:
      config:
       # nacos服务器地址
       server-addr: nacos-server.xxzx:8848      
       group: IOT_DEVICE_GROUP
       #
       encode: UTF-8
       # data-id:
       # 配置文件类型，默认是properties
       prefix: iot-device-service
       file-extension: yaml
      discovery:
        server-addr: nacos-server.xxzx:8848   