spring:   
  profiles:
    active: test #选择使用dev文件 miur.yml，test.yml, pro.yml
  cloud:
    nacos:
      config:
       # nacos服务器地址
       server-addr: nacos-server.xxzx:8848
       # 配置所属分组
       group: DEFAULT_GROUP
       #
       encode: UTF-8
       # data-id:
       # 配置文件类型，默认是properties
       prefix: iot-device-service
       file-extension: yaml
      discovery:
        server-addr: nacos-server.xxzx:8848